# ============================================================
# Фрагмент для config/settings.py
# Скопируйте эти строки в КОНЕЦ вашего settings.py
# ============================================================

# URL-префикс, по которому отдаётся статика
STATIC_URL = 'static/'

# Папки с глобальной статикой проекта (общие для всех приложений)
STATICFILES_DIRS = [
    BASE_DIR / 'static',
]

# Папка, куда collectstatic собирает всю статику для боевого сервера.
# Должна отличаться от папок в STATICFILES_DIRS.
STATIC_ROOT = BASE_DIR / 'staticfiles'
